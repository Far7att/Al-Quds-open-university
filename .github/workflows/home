<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تقنية تصميم صفحات الويب 1266</title>
    <link rel="stylesheet" href="style.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    
    <!-- Navigation -->
    <nav id="main-nav">
        <div class="logo">جامعة القدس المفتوحة</div>
        <button class="hamburger" id="hamburger">☰</button>
        <ul class="nav-links" id="navLinks">
            <li><a href="#home">الرئيسية</a></li>
            <li><a href="#courses">البرامج الدراسية</a></li>
            <li><a href="#campus">الحرم الجامعي</a></li>
            <li><a href="#contact">تواصل معنا</a></li>
            <li><a href="branches.html">الفروع</a></li>
            <li><a href="login.html">تسجيل الدخول</a></li>
        </ul>
    </nav>

    <!-- Back to Top Button -->
    <div class="back-to-top" id="backToTop">↑</div>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="container hero-content">
            <div class="university-card fade-in">
                <h2>جامعة القدس المفتوحة</h2>
                <p>أكمل مسيرتك التعليمية حيث آفاق جديدة تنتظرك لمستقبل مشرق...</p>
                <a href="https://welcomepage643420194.wordpress.com/" class="btn btn-primary">موقعي الشخصي </a>
                <a href="login.html" class="btn btn-secondary">تسجيل الدخول</a>
            </div>
            <div class="welcome-text slide-in-right">
                <h1>مرحبًا<br><span>أكرم فرحات</span></h1>
            </div>
        </div>
        <div class="scroll-indicator" id="scrollToAbout">
            <span>نبذة تعريفية عن الجامعة</span>
            <div class="circle">
                <span>↓</span>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section class="about" id="about">
        <div class="container">
            <h2 class="about-heading">نبذة عن الجامعة</h2>
            <div class="about-content">
                <div class="about-circles">
                    <div class="circle-group">
                        <div class="colored-circle blue-circle"></div>
                        <div class="colored-circle teal-circle"></div>
                        <div class="colored-circle white-circle"></div>
                        <div class="colored-circle pink-circle"></div>
                    </div>
                </div>
                <div class="about-text">
                    <p><span>جامعة القدس المفتوحة</span> طرحت فكرتها عام <span>1975</span> لتلبية احتياجات الطلاب الفلسطينيين، وهي تابعة <span class="highlight">لمنظمة التحرير الفلسطينية</span>. تم تنفيذ الفكرة من خلال <span class="highlight">اليونيسكو</span> عام <span>1980</span>.</p>
                    <button class="btn btn-secondary">الحياة الجامعية</button>
                </div>
                <div class="about-image"></div>
            </div>
        </div>
        <div class="scroll-indicator" id="scrollToCourses">
            <span>البرامج الدراسية</span>
            <div class="circle">
                <span>↓</span>
            </div>
        </div>
    </section>

    <!-- Courses Section -->
    <section class="courses" id="courses">
        <div class="container">
            <h2 class="courses-heading">درجة البكالوريوس</h2>
            <div class="courses-grid">
                <div class="course-card fade-in">
                    <h3>العلوم التربوية</h3>
                    <p>برنامج يركز على تطوير مهارات الطلاب والتعليم للمرحلة الأساسية والعامة.</p>
                    <a href="#" class="btn">التفاصيل</a>
                </div>
                <div class="course-card fade-in">
                    <h3>العلوم الإدارية والاقتصادية</h3>
                    <p>يغطي أساسيات الإدارة، التسويق، والتمويل لتأهيل رواد الأعمال.</p>
                    <a href="#" class="btn">التفاصيل</a>
                </div>
                <div class="course-card fade-in">
                    <h3>الزراعة</h3>
                    <p>برنامج مصمم لتدريب المعلمين على الإنتاج النباتي والحيواني الحديث.</p>
                    <a href="#" class="btn">التفاصيل</a>
                </div>
                <div class="course-card fade-in">
                    <h3>التكنولوجيا والعلوم التطبيقية</h3>
                    <p>يوفر دراسة شاملة أنظمة المعلومات والتطبيقات الحاسوبية والاتصالات والرياضيات.</p>
                    <a href="#" class="btn">التفاصيل</a>
                </div>
            </div>
        </div>
        <div class="scroll-indicator" id="scrollToCampus">
            <span>الحرم الجامعي</span>
            <div class="circle">
                <span>↓</span>
            </div>
        </div>
    </section>

    <!-- Campus Section -->
    <section class="campus" id="campus">
        <div class="container">
            <h2 class="campus-heading">الحرم الجامعي</h2>
            <div class="campus-content">
                <div class="campus-circles">
                    <div class="circle-group">
                        <div class="colored-circle blue-circle"></div>
                        <div class="colored-circle teal-circle"></div>
                        <div class="colored-circle white-circle"></div>
                        <div class="colored-circle pink-circle"></div>
                    </div>
                </div>
                <div class="campus-text slide-in-right">
                    <p>اكتشف <span>الحرم الجامعي</span> التابع لمحافظتك من خلال زيارة <span class="highlight">فروعنا</span>.</p>
                    <a href="branches.html" class="btn btn-secondary">الحياة الجامعية</a>
                </div>
                <div class="map-container slide-in-left">
                    <img src="map.png" alt="Campus Map">
                    <div class="map-pin"></div>
                    <div class="map-overlay">افتح من خلال خرائط جوجل...</div>
                </div>
            </div>
        </div>
        <div class="scroll-indicator" id="scrollToContact">
            <span>تواصل معنا</span>
            <div class="circle">
                <span>↓</span>
            </div>
        </div>
    </section>

    <!-- Branches Section -->
    <section class="branches" id="branches">
        <div class="container">
            <h2 class="branches-heading">فروعنا</h2>
            <div class="branches-grid">
                <div class="branch-card fade-in">
                    <h3>رام الله</h3>
                    <p>الفرع الرئيسي للجامعة في رام الله.</p>
                    <a href="branches.html" class="btn">زيارة الفرع</a>
                </div>
                <div class="branch-card fade-in">
                    <h3>غزة</h3>
                    <p>فرع غزة يقدم جميع البرامج الأكاديمية.</p>
                    <a href="branches.html" class="btn">زيارة الفرع</a>
                </div>
                <div class="branch-card fade-in">
                    <h3>الخليل</h3>
                    <p>فرع الخليل مع خدمات تعليمية متميزة.</p>
                    <a href="branches.html" class="btn">زيارة الفرع</a>
                </div>
            </div>
        </div>
        <div class="scroll-indicator" id="scrollToContact">
            <span>تواصل معنا</span>
            <div class="circle">
                <span>↓</span>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="contact" id="contact">
        <div class="container">
            <h2 class="contact-heading">تواصل معنا</h2>
            <div class="phone-icon">
                <div class="phone-inner"></div>
            </div>
            <form class="contact-form fade-in">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="الاسم الأول" required>
                </div>
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="الاسم الأخير" required>
                </div>
                <div class="form-group">
                    <input type="email" class="form-control" placeholder="البريد الإلكتروني" required>
                </div>
                <div class="form-group">
                    <input type="tel" class="form-control" placeholder="رقم الهاتف" required>
                </div>
                <div class="form-group">
                    <textarea class="form-control" placeholder="رسالتك" rows="5" required></textarea>
                </div>
                <div class="form-actions">
                    <button type="button" class="contact-btn">اختيار المحافظة</button>
                    <button type="submit" class="contact-btn">إرسال</button>
                </div>
            </form>
            <div class="contact-info">
                <p>للمزيد من المعلومات: <a href="mailto:akramabd.aa75@gmail.com">akramabd.aa75@gmail.com</a> أو +970567468915</p>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <ul class="footer-links">
                <li><a href="#home">الرئيسية</a></li>
                <li><a href="#courses">البرامج الدراسية</a></li>
                <li><a href="#campus">الحرم الجامعي</a></li>
                <li><a href="#contact">تواصل معنا</a></li>
                <li><a href="branches.html">الفروع</a></li>
            </ul>
            <p class="copyright">© 2025 جامعة القدس المفتوحة. الطالب أكرم فرحات.</p>
        </div>
    </footer>

    <!-- JavaScript -->
    <script>
        // Toggle Hamburger Menu
        const hamburger = document.getElementById('hamburger');
        const navLinks = document.getElementById('navLinks');

        hamburger.addEventListener('click', () => {
            navLinks.classList.toggle('active');
        });

        // Close menu when clicking a link on mobile
        document.querySelectorAll('.nav-links a').forEach(link => {
            link.addEventListener('click', () => {
                if (window.innerWidth <= 768) {
                    navLinks.classList.remove('active');
                }
            });
        });

        // Navigation scroll effect
        const nav = document.getElementById('main-nav');
        const backToTop = document.getElementById('backToTop');
        
        window.addEventListener('scroll', function() {
            if (window.scrollY > 50) {
                nav.classList.add('scrolled');
                backToTop.classList.add('show');
            } else {
                nav.classList.remove('scrolled');
                backToTop.classList.remove('show');
            }
        });

        // Smooth scroll for navigation and scroll indicators
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Scroll indicators
        document.getElementById('scrollToAbout').addEventListener('click', function() {
            document.getElementById('about').scrollIntoView({
                behavior: 'smooth'
            });
        });

        document.getElementById('scrollToCourses').addEventListener('click', function() {
            document.getElementById('courses').scrollIntoView({
                behavior: 'smooth'
            });
        });

        document.getElementById('scrollToCampus').addEventListener('click', function() {
            document.getElementById('campus').scrollIntoView({
                behavior: 'smooth'
            });
        });

        document.getElementById('scrollToContact').addEventListener('click', function() {
            document.getElementById('contact').scrollIntoView({
                behavior: 'smooth'
            });
        });

        // Back to top button
        document.getElementById('backToTop').addEventListener('click', function() {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });

        // Form validation
        document.querySelector('.contact-form').addEventListener('submit', function(e) {
            e.preventDefault();
            const inputs = this.querySelectorAll('.form-control');
            let isValid = true;
            
            inputs.forEach(input => {
                if (!input.value.trim()) {
                    input.style.borderBottom = '2px solid #e74c3c';
                    isValid = false;
                } else {
                    input.style.borderBottom = '2px solid #2ecc71';
                }
            });

            if (isValid) {
                alert('تم إرسال الرسالة بنجاح!');
                this.reset();
                inputs.forEach(input => {
                    input.style.borderBottom = '2px solid #ddd';
                });
            } else {
                alert('يرجى ملء جميع الحقول المطلوبة');
            }
        });

        // Animation on scroll
        const animateOnScroll = function() {
            const elements = document.querySelectorAll('.fade-in, .slide-in-right, .slide-in-left');
            
            elements.forEach(element => {
                const elementPosition = element.getBoundingClientRect().top;
                const windowHeight = window.innerHeight;
                
                if (elementPosition < windowHeight - 100) {
                    element.style.opacity = '1';
                    element.style.transform = 'translate(0, 0)';
                }
            });
        };

        // Initial setup for animations
        window.addEventListener('load', function() {
            // Set initial state for animations
            document.querySelectorAll('.fade-in').forEach(el => {
                el.style.opacity = '0';
                el.style.transform = 'translateY(20px)';
                el.style.transition = 'opacity 1s ease, transform 1s ease';
            });
            
            document.querySelectorAll('.slide-in-right').forEach(el => {
                el.style.opacity = '0';
                el.style.transform = 'translateX(100px)';
                el.style.transition = 'opacity 1s ease, transform 1s ease';
            });
            
            document.querySelectorAll('.slide-in-left').forEach(el => {
                el.style.opacity = '0';
                el.style.transform = 'translateX(-100px)';
                el.style.transition = 'opacity 1s ease, transform 1s ease';
            });
            
            // Trigger animations
            setTimeout(animateOnScroll, 300);
        });

        window.addEventListener('scroll', animateOnScroll);
    </script>
</body>
</html>
